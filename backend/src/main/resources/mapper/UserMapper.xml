<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xhs.mapper.UserMapper">

    <select id="findByUsername" resultType="com.xhs.entity.User">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <select id="findById" resultType="com.xhs.entity.User">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, avatar, profile)
        VALUES (#{username}, #{password}, #{avatar}, #{profile})
    </insert>

    <update id="update">
        UPDATE user
        <set>
            <if test="password != null">password = #{password},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="profile != null">profile = #{profile},</if>
        </set>
        WHERE id = #{id}
    </update>

    <select id="search" resultType="com.xhs.entity.User">
        SELECT * FROM user 
        WHERE username LIKE CONCAT('%', #{keyword}, '%') 
        OR profile LIKE CONCAT('%', #{keyword}, '%')
        ORDER BY created_at DESC
    </select>

</mapper>
